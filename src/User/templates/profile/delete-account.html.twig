<h3>Delete account</h3>
<span class="sub-title"></span>

{{ messagesPartial('partial::alerts', {}, null, 'profile-delete') }}

{{ form().openTag(form)|raw }}

<div class="checkbox delete-checkbox">
    {{ formElement(form.get('isDeleted')) }}
    <p>I accept the <a href="#" target="_blank">Terms and Conditions</a> and I want to delete my account with all additional data.</p>
</div>

{{ formElement(form.get('userDeleteCsrf')) }}

{{ formSubmit(form.get('submit')) }}

{{ form().closeTag()|raw }}
