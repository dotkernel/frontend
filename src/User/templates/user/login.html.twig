{% extends '@layout/default.html.twig' %}

{% block title %}Login{% endblock %}

{% block content %}
    <div>
        <h1>Login</h1>
        {% for element in form.elements %}
            {{ formElement(element) }}
        {% endfor %}
    </div>
{% endblock %}
